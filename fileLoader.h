#include <iostream>
#include <list>
#include "./Simulation/Student.h"

using namespace std;

int readWindows(string file);
// Reads a file to get the number of windows from it

list<Student> readStudents(string file);
// Reads a file to get the students from it
